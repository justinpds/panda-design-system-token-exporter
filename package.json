{
  "name": "panda-design-tokens",
  "version": "2.0.0",
  "description": "Panda Design System – DTCG + Figma token export from Notion",
  "type": "module",
  "scripts": {
    "export": "node export-tokens.mjs",
    "export:approved": "node export-tokens.mjs --approved-only",
    "export:dtcg": "node export-tokens.mjs --dtcg-only",
    "export:figma": "node export-tokens.mjs --figma-only",
    "export:dry-run": "node export-tokens.mjs --dry-run"
  },
  "engines": {
    "node": ">=18"
  },
  "private": true
}
